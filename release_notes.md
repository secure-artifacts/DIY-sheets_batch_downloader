# DIY下载器 v1.2.0

## 新增：批量上传到 Google 云端

新增独立标签页 **「批量上传云端」**，对齐原 Web「多任务批量文件上传系统」流程：

- 从表格 **分类目录** 读取一/二/三级分类（空白向下继承）
- **视频/文件分类模式**：`一级/二级/三级/日期/上传者`
- **图片直传模式**：`图片素材/日期/上传者`
- 选择本地文件或整夹，加入多任务清单后批量上传
- 自动在 Drive 父文件夹下创建层级目录
- 同名文件可跳过/复用
- 写入 **入库表**（第 7 行上方插入，字段对齐原脚本）
- 写入 **上传日志**
- 版权声明勾选

### 权限说明

上传需要 Google Drive **写入**权限。若你之前只下过文件：

1. 关闭程序  
2. 删除 `token.json`  
3. 重新打开并完成 OAuth 授权  

服务账号请把父文件夹共享给服务账号邮箱（编辑权限）。

## 其它

- 继承 v1.1.x：onedir 安装、画质修复、环境一键装 ffmpeg、CodeQL 域名校验修复
