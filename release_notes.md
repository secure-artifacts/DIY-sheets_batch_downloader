# DIY下载器 v1.4.0

## 粘贴链接下载（独立板块）

- 新增顶部标签 **「粘贴链接下载」**，与表格整列扫描分离
- 读取表格 **名称列 + 链接列**（均可配置，如 A / C）
- 粘贴链接后直接点「开始下载」：自动按链接列匹配行号 → 下载 → 回填
- 支持 Google Drive **文件夹**与单文件；文件夹递归解析文件数量并下载

### 回填（列均可配置）

- 文件夹文件总数量
- 下载状态：`正在下载` → `已下载完成`
- 下载人员
- 完成日期（`YYYY-MM-DD`）

### 暂停 / 断点 / 排重

- **暂停 / 继续**
- **断点续传**：文件夹内已下文件跳过，只补未完成部分
- **按 Drive 文件/文件夹 ID 排重**：同一内容不重复下载（`.diy_drive_ids.json`）

## 其它

- 表格下载：默认整列扫描，去掉起止行自定义
- 全局设置（凭据）、上传独立表格 ID、界面白底/弹窗对比度等沿用 v1.3
