# DIY下载器 v1.4.2

## 批量上传：失败重试与清单记忆

- **自动重试**：网络抖动 / 5xx / 限流等，单文件最多自动重试 3 次
- **空间已满等错误不重试**（`storageQuotaExceeded`），并在日志提示清理空间
- **重试失败项**：只上传未成功文件，已成功的会跳过
- **清单自动保存**到本机 `%LOCALAPPDATA%\DIYDownloader\upload_task_queue.json`
- **下次启动自动恢复**上次上传清单（含失败状态）
- **清除已完成**：去掉成功/跳过项，只保留失败与待上传，便于继续重试
- **清空清单**会同步删除本机保存的队列

## 沿用 v1.4.1

- 成品入库表 F/G/H（清理元数据 / 审核 / 上传者）及列、表名可配置
- 左侧设置区加宽；Google 连接失败中文说明与重试
