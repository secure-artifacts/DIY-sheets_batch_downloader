# DIY下载器 v1.4.4

## 授权：修复每次加载都要重新登录

- 已有 `token.json` 时优先 **复用本机授权**，不再每次弹浏览器
- access token 过期时用 `refresh_token` **静默续期** 并写回本机
- **网络刷新失败不再误判为需重新授权**（避免 Connection aborted 后反复登录）
- 真正需要重登时：缺少 refresh_token、权限不足、refresh 被吊销等
- 授权保存失败会明确报错（不再静默失败导致“永远记不住登录”）
- 日志显示授权状态：`复用本机授权` / `已自动续期` / `刚重新登录`

## 沿用

- v1.4.3 清理配置 / 重新授权入口
- v1.4.2 上传失败重试与清单记忆
- v1.4.1 成品入库表列配置
- v1.4.0 粘贴链接下载
