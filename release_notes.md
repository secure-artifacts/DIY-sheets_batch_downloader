# DIY下载器 v1.5.1

## 🐛 紧急修复与稳定性增强 (Bug Fixes)

- **修复 SSL Socket 线程冲突 (`[SSL: BAD_LENGTH]`) 与意外退出**：
  - 为多线程并发 Worker 引入 `threading.local` 独立客户端隔离
  - 彻底解决了多线程争抢底层 SSL Socket 导致的 `[SSL: BAD_LENGTH]` 报错及 C 级别进程闪退问题
- **修复上传者/审核人员被覆盖问题**：
  - 任务加入清单时锁定当次保存的「上传者」与「审核人员」名字
  - 修复了多次添加不同人（如张三、李四）时，后续添加会重写覆盖之前任务名字的 Bug
  - 表格清单直接显示 `[张三] 分类目录` 标识，任务归属一目了然
- **优化拖拽（DropZone）与防丢文件体验**：
  - 优化 Windows 绝对路径规范化，拖入文件/分类文件夹立即入库
  - 即使未提前点击「加载分类目录」，拖入文件夹也能自动结合当前界面选择入库，绝不丢弃任何文件

## ⚡ v1.5.0 传输性能大幅提速 (Speed Engine)

- **8MB 极速分块**：Google Drive 上传与下载 Chunk Size 提升至 8MB
- **多线程并发传输**：支持 1~16 线程并发传输（顶栏集成 ⚡并发 控制）
- **内存级目录去重**：云端文件夹全量清单预读缓存

## 🎨 UI 视觉与交互重构

- **全新暗色科技风 UI**：现代面板、圆角卡片、流线型输入框与高亮 Tag 标签
- **全局控制顶栏**：集成并发数调节、全局凭据设置与极速模式
- **智能可折叠面板**：
  - 「批量上传云端」：常驻上传者与审核人员，其他设置可折叠
  - 「粘贴链接下载」：常驻人员姓名与保存目录，高级配置可折叠
  - 「表格下载」：常驻关键字筛选与保存目录，表格细节可折叠

## 沿用功能

- v1.4.3 全局配置清理与重新授权
- v1.4.2 上传失败重试与清单记忆
- v1.4.0 粘贴链接下载（暂停、断点、ID 排重）
